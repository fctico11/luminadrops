/** While true, the Back Room shows only its "door is locked" message — no
 * board, no DB reads, and the post/like server actions refuse to run.
 * Flip to false to open the community board. */
export const BACK_ROOM_LOCKED = true;
